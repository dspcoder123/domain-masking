# Masking Demo

This project hosts a Render application inside an iframe using Netlify.

## Structure

- `index.html` - Displays the Render application
- `netlify.toml` - Netlify deployment configuration

## Render URL

Update the `src` attribute in `index.html` with the Render application URL.

Example:

https://my-demo-app.onrender.com